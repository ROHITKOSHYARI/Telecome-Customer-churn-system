spring:
  datasource:
    url: jdbc:postgresql://ep-bitter-waterfall-aob26o8j-pooler.c-2.ap-southeast-1.aws.neon.tech/customerchurn?user=neondb_owner&password=npg_fWjXZAKh37RC&sslmode=require&channelBinding=require
    username: postgres

  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  application:
    name: customerChurn

logging:
  level:
    org:
      springframework:
        security: TRACE
ml:
  service:
    url : http://churn-ml:8000/predict

jwt:
  secret: ${JWT_SECRET:change-this-development-secret-before-deploying}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
